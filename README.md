# Task Tracker - единый инструмент управления задачами

Самодостаточный TUI + CLI инструмент для управления задачами проекта. Один файл, простые команды, мгновенное отображение прогресса.

**Для AI:** См. [AI.md](AI.md) — компактный формализованный справочник.

## Быстрый старт

```bash
# Установка зависимостей
pip install -r requirements.txt

# Установка apply_task в PATH (один раз)
cp apply_task ~/.local/bin/ && chmod +x ~/.local/bin/apply_task

# Перезапустить терминал или выполнить
source ~/.zshrc  # или ~/.bashrc

# Запуск TUI
apply_task tui
```

## 🚀 Формализованные команды

**Принцип**: Детерминированный синтаксис. Одна операция — одна команда.

### Структурированный вывод

- Каждая неинтерактивная команда `apply_task` печатает единый JSON-блок с полями `command`, `status`, `message`, `timestamp`, `summary`, `payload`.
- `payload.task`/`payload.tasks` содержат полные данные задач и подзадач (критерии, тесты, блокеры, заметки чекпоинтов).
- Ошибки возвращаются с `status: "ERROR"` в том же формате. Интерактивные сценарии (`guided`, `tui`) продолжают работать в текстовом режиме.
- Это избавляет от парсинга человекочитаемых списков: AI/скрипты читают/пишут JSON напрямую.

```bash
apply_task "Fix memory leak #bug #critical"    # Создать задачу
apply_task show                                 # Показать последнюю
apply_task show 001                             # Показать TASK-001
apply_task list                                 # Список задач
apply_task start                                # Начать → WARN
apply_task done                                 # Завершить → OK
apply_task fail                                 # Провалить → FAIL
apply_task next                                 # Следующая задача
apply_task tui                                  # TUI интерфейс

# Workflow
apply_task "Task #tag" → apply_task show → apply_task start → apply_task done
```

**Smart парсинг:**
- `#тег` → автоматически извлекается в теги
- `@TASK-XXX` → автоматически в зависимости

**Контекст последней задачи:**
- Не нужно запоминать ID
- `apply_task show` → показать последнюю
- `apply_task done` → завершить последнюю

**Привязка к git проекту:**
- Работает из любой поддиректории проекта
- Автоматически находит tasks.py в корне git
- Все задачи в одном месте (корень проекта)

```bash
# Работает из любой вложенности
cd my-project/src/components/auth/hooks
apply_task "Add useAuth #feature"    # → создаст в my-project/.tasks/

cd ../../api/controllers
apply_task list                       # → покажет из my-project/todo.machine.md
```

См. [GIT_PROJECT.md](GIT_PROJECT.md) для деталей.

## Ключевые особенности

- ✓ **Единый файл** - просто скопируй `tasks.py` в любой проект
- ✓ **Привязка к git** - работает из любой поддиректории, находит tasks.py в корне git
- ✓ **TUI + CLI** - визуальный интерфейс и команды для автоматизации
- ✓ **Глобальная команда** - `apply_task` доступна из любой директории в PATH
- ✓ **Русская раскладка** - все горячие клавиши работают на обеих раскладках
- ✓ **Кэширование** - мгновенная навигация по задачам
- ✓ **Навигация по зависимостям** - переходы между связанными задачами
- ✓ **Гибкий ввод подзадач** - `--subtasks @file.json` читают из файла, `--subtasks -` — из STDIN без экранирования
- ✓ **Готовые шаблоны** - `apply_task template subtasks --count N` генерирует JSON каркасы под требуемое число подзадач
- ✓ **Макросы и история** - `apply_task ok|note|bulk` ускоряют чекпоинты, `apply_task history/replay` помогают повторять команды
- ✓ **Валидация без записи** — `apply_task create|task ... --validate-only` прогоняет все проверки, не создавая `.task`
- ✓ **Guided чекпоинты** — `apply_task checkpoint [TASK]` шаг за шагом закрывает критерии/тесты/блокеры (есть `--auto`)
- ✓ **Доменная дисциплина** — все задачи создаются с `--domain/-F`, см. [DOMAIN_STRUCTURE.md](DOMAIN_STRUCTURE.md)

## Команды (для AI и автоматизации)

### Основные команды apply_task

```bash
apply_task tui                      # TUI интерфейс
apply_task list                     # Список всех задач
apply_task show                     # Детали последней задачи
apply_task show 001                 # Детали TASK-001
apply_task "Fix bug #critical"      # Создать задачу
apply_task start                    # Начать работу (→ WARN)
apply_task done                     # Завершить (→ OK)
apply_task next                     # Следующая задача
apply_task ok TASK-ID IDX           # Критерии/тесты/блокеры + done одним вызовом
apply_task note TASK-ID IDX ...     # Быстрое добавление заметки/чекпоинта
apply_task bulk --input payload.json# Batch чекпоинты по JSON
apply_task checkpoint TASK-ID --auto# Guided мастер по чекпоинтам (CLI/TUI)
apply_task "Task" --parent ROOT --tests unit --risks perf --subtasks ... --validate-only  # dry-run проверки
apply_task history [N]              # История команд
apply_task replay N                 # Повторить команду из истории
```

### Прямые команды tasks.py (для скриптов)

```bash
./tasks.py tui                      # TUI
./tasks.py list                     # Список
./tasks.py list --status WARN       # Фильтр по статусу
./tasks.py tui --theme dark-contrast   # TUI с альтернативной темой
./tasks.py show TASK-001            # Показать детали
./tasks.py create "New task"        # Создать
./tasks.py task "Task #tag"         # Создать + smart парсинг (теги/зависимости)
./tasks.py update TASK-001 OK       # Обновить
./tasks.py analyze TASK-001         # Анализ
./tasks.py next                     # Следующая задача
```

## TUI горячие клавиши

### Основные
- `q` / `й` / `Ctrl+Z` - выход
- `r` / `к` - reload
- `Enter` - открыть детали / перейти к dependency
- `Esc` - назад

### Навигация
- `↑↓` / `j`/`о` `k`/`л` - вверх/вниз
- Колёсико мыши - перемещение выделения (в списке задач и внутри деталей)
- Работает для выбора подзадач и dependencies

### Фильтры
- `0` - все задачи
- `1` - только OK
- `2` - только WARN
- `3` - только FAIL

## Структура данных

### todo.machine.md (главный список)

```markdown
## Category

- [x] Task name | OK | Description >> .tasks/TASK-001.task
```

Формат: `- [статус] Название | STATUS | Описание >> файл.task`
- Статус: `[x]` завершено, `[!]` в процессе, `[ ]` не начато
- STATUS: `OK`, `WARN`, `FAIL`

### .tasks/TASK-XXX.task (детали)

```markdown
---
id: TASK-001
title: Task name
status: OK
priority: HIGH
---

## Подзадачи
- [x] Completed
- [ ] Todo

## Зависимости
- TASK-002 (другая задача)
```

## Копирование в другой проект

```bash
# Скопируй файлы в проект:
cp tasks.py /path/to/project/
cp requirements.txt /path/to/project/

# Создай структуру:
cd /path/to/project
git init  # если ещё не git проект
mkdir .tasks
touch todo.machine.md

# Готово! Используй (apply_task уже в PATH):
apply_task tui
apply_task "First task #setup"
```

## Примеры для AI

### Быстрое создание

```bash
apply_task "Fix memory leak #bug #critical"
# ✓ Создана TASK-020: Fix memory leak
#   Теги: bug, critical
#   Добавь подзадачи: apply_task subtask 020 \
#       --add "Wire metrics export >=20 chars" \
#       --criteria "p95<=120ms;grafana dashboard" \
#       --tests "pytest -q tests/api/test_metrics.py" \
#       --blockers "SRE approval;feature flag on"
```

### Создание с зависимостями

```bash
apply_task "Implement OAuth2 @TASK-015 #feature #backend"
# ✓ Создана TASK-021: Implement OAuth2
#   Теги: feature, backend
#   Зависимости: TASK-015
#   Добавь подзадачи: apply_task subtask 021 \
#       --add "Implement OAuth redirect + scopes validation" \
#       --criteria "spec signed;scopes map published" \
#       --tests "pytest -q tests/oauth/test_flow.py;newman run oauth.json" \
#       --blockers "Auth0 tenant;frontend integration"
```

### Работа с задачей

```bash
# Создать
apply_task "Refactor payment processor #refactoring #critical"

# Посмотреть
apply_task show

# Начать
apply_task start

# Завершить
apply_task done

# Следующая
apply_task next
```

### Отслеживание прогресса

```bash
# Проверить статус
apply_task list

# Вывод:
# ✓ TASK-001: Auth backend [OK] 100%
# ! TASK-002: OAuth2 flow [WARN] 33%
# ✗ TASK-003: Token refresh [FAIL] 0%

# Выбрать следующую задачу
apply_task next

# Вывод: рекомендует топ-3 задачи по приоритету
```

## Производительность

- Кэширование распарсенных файлов (~1800x)
- Кэширование проверки существования (~7x)
- Кэширование рендера
- Мгновенная навигация даже с сотнями задач

## Зависимости

```txt
prompt_toolkit>=3.0.0
PyYAML>=6.0
```

## Файловая структура

```
project/
├── tasks.py              # Единый исполняемый файл (44KB)
├── todo.machine.md       # Главный список задач
├── .tasks/               # Детальные файлы
│   ├── TASK-001.task
│   └── TASK-002.task
└── requirements.txt
```

## Лицензия

MIT
- `apply_task ... --domain payments/refunds` — укажи домен/подпапку `.tasks` (подробнее в DOMAIN_STRUCTURE.md)
